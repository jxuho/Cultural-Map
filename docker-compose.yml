services:
  mongodb:
    image: mongo:latest
    container_name: mongodb_dev
    ports:
      - "27018:27017"
    command: ["--replSet", "rs0"]
    volumes:
      - mongo_data:/data/db
volumes:
  mongo_data: